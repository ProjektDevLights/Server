Alarm planen: zeit, farbe, tage, an?
  prüfen ob andere in quere kommen *1
	alles in db speichern //leider immer zu spät
	cronjob starten (alarmid, callback (allgemein für alarms))

alarm bearbeiten:
  Alarm planen[0,1]
  alten job zerstören
  Alarm planen[2]


server neustart:
  alle die an sind --> neue cronjobs planen



*1 wenn ja nicht erlauben
  andere alarms mit min. 1 gleichem licht überprüfen --> konfliktlicher <-> alarm

callback:
  startet fading

cron:
  keine tage --> an jedem tag, solange der alarm angeschaltet bleibt
  tage -->  immer an tagen wenn an  /kill alarm gibts nur vom user


ALAAAAAAAARRRRRRRRRM (es kommt ein Notruf an, Feuerwehrmann Sam ist unser Mann!):
  an,
  farbe,
  uhrzeit,
  cronpattern, // nur in db
  lichter,
  (name)


methoden:
  planen(zeit,farbe,lichter[uuids])
  cronPlanner([alarm]id,cronpattern,callback*)
  *callback([alarm]id)
  edit(id, [neue daten])













mitdenken statt querdenken
~WAT was?
~WHAT~~~~~~~~~~~~~~~~~~~~~~







TODO Alarm mit Gedankenchip ein und ausschalten





timo stinkt
